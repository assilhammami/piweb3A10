<form method="post" action="{{ path('app_likes_delete', {'likeId': like.likeId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ like.likeId) }}">
    <button class="btn">Delete</button>
</form>
