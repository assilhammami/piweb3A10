

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des commentaires</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        /* Styles généraux */
        body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background: url('{{ asset('Admin/img/logo.jpg') }}') no-repeat center center fixed;
    background-size: cover;
    color: #333;
    width: 100%; /* Assure que le body couvre toute la largeur de la page */
    height: 100%; /* Assure que le body couvre toute la hauteur de la page */
}


        /* En-tête */
        header {
            background-color: #009CFF;
            color: #fff;
            padding: 20px;
            text-align: center;
        }

        header h1 {
            margin: 0;
        }

        /* Contenu principal */
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        /* Tableau */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 10px;
            border: 1px solid #ddd;
            text-align: left;
        }

        th {
            background-color: #009CFF;
            color: #fff;
            font-weight: bold;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        tr:hover {
            background-color: #f1f1f1;
        }

        /* Logo */
        .logo {
            width: 140px;
            height: auto;
            border-radius: 10px;
            box-shadow: 3px 3px 8px rgba(0, 0, 0, 0.3);
        }

        /* Titre */
        h2 {
            text-align: center;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>

<header>
    <h1>Liste des commentaires</h1>
</header>
 
<div class="container">
    <!-- Image de logo -->
    <div style="text-align: center; margin-bottom: 20px;">
        <img src="{{ asset('Admin/img/logo.jpg') }}" class="logo" alt="Logo">
    </div>

    <!-- Tableau des commentaires -->
    <table>
        <thead>
            <tr>
                <th>Commentaires</th>
                <th>IdUser</th>
                <th>IdCours</th>
            </tr>
        </thead>
        <tbody>
            {% for avis in avis %}
                <tr>
                    <td>{{ avis.commentaire }}</td>
                    <td>{{ avis.id_user }}</td>
                    <td>{{ avis.id_cour }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

</body>
</html>


<div style="display: flex; justify-content: space-between; align-items: center;">
    <img src="{{ asset('frontoffice/images/logo1.jpg') }}" style="width: 140px; height: 100px;" alt="Description de votre image">
</div>

<br>
<h2>Liste des archives</h2> <!-- Ajout du titre ici -->


<div style="display: flex; justify-content: space-between; align-items: center;">
    <img src="{{ asset('imagee/logo.jpg') }}" style="width: 140px; height: 100px;" alt="Description de votre image">
</div>

<br>
<h2>Liste des réservations</h2> <!-- Ajout du titre ici -->

<br>

<table border="1">
    <thead>
        <tr>

            <th style="color:#009CFF;">date</th>
            <th style="color:#009CFF;">image</th>
            <th style="color:#009CFF;">description</th>
        </tr>
    </thead>
    <tbody>
        {% for archives in archives %}
            <tr>
           <td>{{ archives.dateCreation ? archives.dateCreation|date('Y-m-d') : '' }}</td>
                <td>{{ archives.image }}</td>
                <td>{{ archives.description }}</td>

            <th style="color:#009CFF;">Id</th>
            <th style="color:#009CFF;">Date</th>
            <th style="color:#009CFF;">NbPlaces</th>
        </tr>
    </thead>
    <tbody>
        {% for reservation in reservations %}
            <tr>
                <td>{{ reservation.id }}</td>
                <td>{{ reservation.date }}</td>
                <td>{{ reservation.nbplaces }}</td>

            </tr>
        {% endfor %}
    </tbody>
</table>
<style>
/* Styles pour l'image de logo */
img.logo {
    width: 140px;
    height: 100px;
    object-fit: contain; /* Pour s'assurer que l'image s'ajuste à la zone définie sans se déformer */
    border-radius: 10px; /* Ajoute des coins arrondis à l'image */
    box-shadow: 3px 3px 8px rgba(0, 0, 0, 0.3); /* Ajoute une ombre subtile à l'image */
}
/* Styles pour le tableau */
table {
    width: 100%; /* Le tableau occupe toute la largeur de la page */
    border-collapse: collapse; /* Pour éviter les espaces entre les cellules */
    margin-bottom: 20px; /* Espace en bas du tableau */
}

th, td {
    padding: 10px; /* Ajoute de l'espace intérieur aux cellules */
    border: 1px solid #ddd; /* Ajoute une bordure aux cellules */
    text-align: left; /* Aligne le texte à gauche */
}

th {
    background-color: #009CFF; /* Couleur de fond des en-têtes */
    color: white; /* Couleur du texte des en-têtes */
    font-weight: bold; /* Gras pour les en-têtes */
}

tr:nth-child(even) {
    background-color: #f9f9f9; /* Couleur de fond alternée pour les lignes paires */
}

tr:hover {
    background-color: #f1f1f1; /* Couleur de fond lors du survol des lignes */
}
/* Alignement centré pour le titre et autres éléments */
h2, table {
    text-align: center;
}

/* Amélioration de l'alignement du logo */
.logo-container {
    display: flex;
    justify-content: center; /* Centrer l'image */
    align-items: center;
    margin-bottom: 20px; /* Ajoute un espace en dessous de l'image */
}

</style>

</style>



