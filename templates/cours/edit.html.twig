{% extends 'base.html.twig' %}

{% block title %}Edit Cours{% endblock %}

{% block body %}
    <h1>Edit Cours</h1>

    {{ include('cours/_form.html.twig', {'button_label': 'Update'}) }}

     <button type="button" class="btn btn-primary" onclick="window.location.href='{{ path('app_cours_index') }}'">Back to list</button>

    {{ include('cours/_delete_form.html.twig') }}
{% endblock %}
