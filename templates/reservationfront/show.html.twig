{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reservation.date }}</td>
            </tr>
            <tr>
                <th>Nbplaces</th>
                <td>{{ reservation.nbplaces }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservationfront_index') }}">back to list</a>

    <a href="{{ path('app_reservationfront_edit', {'id': reservation.id}) }}">edit</a>

    {{ include('reservationfront/_delete_form.html.twig') }}
{% endblock %}
