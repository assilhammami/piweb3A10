<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Travail</title>
    <style>
        /* Styles pour les champs de formulaire */
        .form-control {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-bottom: 10px; /* Ajout d'un espacement en bas de chaque champ */
        }

        /* Style pour le bouton Soumettre */
        .btn-submit {
            display: inline-block;
            padding: 10px 20px;
            font-size: 16px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .btn-submit:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
        {{ form_widget(form) }}
        <button class="btn-submit">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
</body>
</html>
