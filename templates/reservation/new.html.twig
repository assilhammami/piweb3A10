{% extends 'backoffice/base.html.twig' %}
{% block title %}New Reservation{% endblock %}

{% block body %}
    <h1>Create new Reservation</h1>

    {{ include('reservation/_form.html.twig') }}

    <a href="{{ path('app_reservation_index') }}">Back to list</a>

    {# Ajouter un lien pour créer une nouvelle réservation pour un événement spécifique #}
    {% if event is defined %}
        <a href="{{ path('app_reservation_new', {'eventId': event.id}) }}">Add reservation for {{ event.nom }}</a>
    {% endif %}
{% endblock %}